on:
  push:
    branches:
      - main
      - master
    paths:
      - 'src/**'
      - 'workflows/**'

  pull_request:
    branches:
      - main
      - master
jobs:
  test:
     runs-on: unbuntu-latest
     steps:
      - name: Checkout code
        uses: actions/checkout@v3
    
      - name: Run tests 
        run: |
         echo "Running tests..."
        # Add your test commands here
